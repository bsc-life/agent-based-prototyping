Unified schema entrypoints

This package provides a single import location for the existing schema variants.
All classes are thin wrappers over the current implementations to preserve
behavior without modifying the original folders.
